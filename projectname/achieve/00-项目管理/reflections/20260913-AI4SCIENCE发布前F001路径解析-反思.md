---
title: "AI4SCIENCE 发布前 F001 路径解析反思"
document_id: "REF-20260913-AI4SCIENCE-F001-001"
produced_at: "2026-09-13"
status: "已澄清"
owner: "项目监理、工程责任人"
---

# AI4SCIENCE 发布前 F001 路径解析反思

## 1. 目标与事实

- 关联需求/问题 ID：SR-REL-001 / QG-20260913-AI4SCIENCE-RELEASE-001。
- 预期结果：按根级项目配置执行 `scripts/f001_governance_validate.py --project-root <AI4SCIENCE 根>` 并读取已配置的 `projectname/project/configs/features.yaml`。
- 实际结果：将根项目目录误传给工程级脚本时，脚本尝试读取 `<AI4SCIENCE 根>/configs/features.yaml` 并失败；按工程级规则在 `projectname/project` 中以 `--project-root .` 执行后通过，退出码为 0。
- 证据：本次执行的 Python traceback；根级 `AGENTS.md` 的 `feature_registry = "projectname/project/configs/features.yaml"`；实际 `projectname/project/configs/features.yaml`。

## 2. 偏差与根因

| 偏差 | 根因类别 | 已证事实 | 推测 | 影响 |
|---|---|---|---|---|
| 将根项目目录传给工程级 F001 | 流程/调用范围错误 | 工程级规则明确命令在工程根使用 `--project-root .`；按该命令检查通过 | 无 | 初始 L1 失败，复核后解除 |

## 3. 可复用规律

- 适用范围：含嵌套 `projectname/project` 工程根的校验器调用。
- 不适用范围：不含根级配置或功能注册表不在工程根的独立仓库。
- 同类历史证据：无。
- 与现有规则的关系：工程级规则已定义正确调用范围；本次未按该范围调用。

## 4. 最小改进与再验证

| 改进对象 | 责任人 | 路径 | 权限 | 验证 | 回滚 |
|---|---|---|---|---|---|
| 发布前先按目标脚本作用域核对命令 | 项目监理 | 发布检查记录 | 无需修改工程脚本 | 在工程根运行 F001 已通过 | 不适用 |

- 是否提议 Skill/规范演化：否；这是一次调用范围误用，已通过复核纠正。
- 人工决定：无需工程修复；发布使用工程级 F001 通过结果。
